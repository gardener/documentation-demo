import{_ as o,c as t,o as i,a2 as s}from"./chunks/framework.HBgHkVyW.js";const p=JSON.parse('{"title":"Input Validation","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener-extension-shoot-rsyslog-relp","github_subdir":"docs/development","params":{"github_branch":"main"},"path_base_for_github_subdir":{"from":"content/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation.md","to":"input-validation.md"},"persona":"Developers","title":"Input Validation","prev":false,"next":false},"headers":[],"relativePath":"docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation/index.md","filePath":"docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation.md","lastUpdated":null}'),r={name:"docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation/index.md"};function a(n,e,d,l,c,h){return i(),t("div",null,e[0]||(e[0]=[s('<h1 id="validation-guidelines" tabindex="-1">Validation Guidelines <a class="header-anchor" href="#validation-guidelines" aria-label="Permalink to &quot;Validation Guidelines&quot;">​</a></h1><p>This document provides developer guidelines on validation practices and conventions used in the <code>gardener-extension-shoot-rsyslog-relp</code> codebase so that the rsyslog and auditd configurations on the Shoot nodes behave as expected and are not vulnerable to malicious input.</p><p>For general guidelines on input validation in Gardener, refer to the <a href="/pr-preview/pr-792/docs/gardener/validation-guidelines/">Validation Guidelines</a> document in the Gardener repository.</p><h2 id="validation-in-the-shoot-rsyslog-relp-extension" tabindex="-1">Validation in the <code>shoot-rsyslog-relp</code> extension <a class="header-anchor" href="#validation-in-the-shoot-rsyslog-relp-extension" aria-label="Permalink to &quot;Validation in the `shoot-rsyslog-relp` extension&quot;">​</a></h2><p>The <code>shoot-rsyslog-relp</code> extension exposes two APIs, which can be used to configure the rsyslog and auditd daemons running on Shoot nodes:</p><ul><li><a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/apis/rsyslog/types_rsyslog.go" target="_blank" rel="noreferrer"><code>RsyslogRelpConfig</code></a> is used to configure rsyslog and is provided as an entry in the <code>spec.extensions</code> field array of Gardener Shoots. See the <a href="/pr-preview/pr-792/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/configuration/#shoot-configuration">Shoot Configuration</a> documentation for more information.</li><li><a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/apis/rsyslog/types_auditd.go" target="_blank" rel="noreferrer"><code>Auditd</code></a> is used to configure audit rules and is provided as a data field in a ConfigMap which must be referenced from the <code>RsyslogRelpConfig</code>. See the <a href="/pr-preview/pr-792/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/configuration/#configuring-the-audit-daemon-on-the-shoot-nodes">Configuring the Audit Daemon on the Shoot Nodes</a> documentation for more information.</li></ul><p>Validation of these APIs is enforced by an admission webhook that acts on create and update requests for Shoots.</p><p>When processing a create or update request for a Shoot, the admission identifies the <code>shoot-rsyslog-relp</code> extension entry in the Shoot&#39;s <code>spec.extensions</code> array by looking for an entry that has type <code>shoot-rsyslog-relp</code>. If such an entry does not exist, or if the extension is disabled, the validation is skipped.</p><p>When the <code>shoot-rsyslog-relp</code> extension entry is present and is not disabled, the validator decodes the entry&#39;s <code>providerConfig</code> field into an <code>RsyslogRelpConfig</code> struct by using strict decoding, so that proper schema validation is performed.</p><p>The <code>RsyslogRelpConfig</code> struct is validated according to the rules specified in the <a href="/pr-preview/pr-792/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation/#rsyslog-config-file">Rsyslog Config File</a> section below, including regex validation of string fields and the presence of required fields when features are enabled.</p><p>If a Secret that contains the TLS certificates, or a ConfigMap that contains the <code>Auditd</code> settings are provided, they are also validated by this admission webhook. More information on those is available in the <a href="/pr-preview/pr-792/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation/#tls-secret">TLS Secret</a> and <a href="/pr-preview/pr-792/docs/extensions/others/gardener-extension-shoot-rsyslog-relp/input-validation/#auditd-configuration"><code>Auditd</code> Configuration</a> sections, respectively.</p><p>Note that the Secret and ConfigMap are enforced to be immutable. This is necessary because the admission webhook only reacts to Shoot updates, not to updates of the referenced resources. When the user wants to change something in either the Secret or the ConfigMap, new resources must be created and the corresponding entries in the Shoot&#39;s <code>spec.resources</code> array field must be updated to point to the new resources. This Shoot update triggers the admission webhook, which validates both the updated Shoot configuration and the newly referenced resources.</p><p>For more details on the implementation of the admission webhook check the <a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/admission/validator/shoot.go" target="_blank" rel="noreferrer"><code>pkg/admission/validator/shoot.go</code></a> file.</p><p>Any new APIs added in the future must be validated in a similar way.</p><h3 id="validation-of-rsyslog-and-auditd-configuration" tabindex="-1">Validation of Rsyslog and Auditd Configuration <a class="header-anchor" href="#validation-of-rsyslog-and-auditd-configuration" aria-label="Permalink to &quot;Validation of Rsyslog and Auditd Configuration&quot;">​</a></h3><p>The <code>shoot-rsyslog-relp</code> extension installs a couple of files on the Shoot&#39;s nodes that could be configured by user input via the <a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/apis/rsyslog/types_rsyslog.go" target="_blank" rel="noreferrer"><code>RsyslogRelpConfig</code></a> and <code>Auditd</code> APIs. These are:</p><ul><li>The <code>/var/lib/rsyslog-relp-configurator/rsyslog.d/60-audit.conf</code> file, which contains the rsyslog configuration.</li><li>Certificate authority, client certificate and private key for the tls connection to the rsyslog target server.</li><li>Audit rules files under the <code>/var/lib/rsyslog-relp-configurator/audit/rules.d</code> directory.</li><li>The <code>/var/lib/rsyslog-relp-configurator/configure-rsyslog.sh</code> script, which copies certificates and rsyslog configuration file and audit rules files to their corresponding directories under <code>/etc</code>.</li></ul><h4 id="rsyslog-config-file" tabindex="-1">Rsyslog Config File <a class="header-anchor" href="#rsyslog-config-file" aria-label="Permalink to &quot;Rsyslog Config File&quot;">​</a></h4><p>Rsyslog configuration files can contain and run code written in <a href="https://www.rsyslog.com/doc/rainerscript/index.html" target="_blank" rel="noreferrer">RainerScript</a>. This code could be used to read, modify or execute other files with the permissions of the rsyslog process. Special care must be taken to ensure that no malicious code can be injected in the <code>/var/lib/rsyslog-relp-configurator/rsyslog.d/60-audit.conf</code> file.</p><p>Fields in the <code>RsyslogRelpConfig</code> API must be validated and their values must be properly escaped before they are passed to the <code>/var/lib/rsyslog-relp-configurator/rsyslog.d/60-audit.conf</code> configuration file.</p><p>The following validation rules must be strictly followed:</p><p><strong>String Field Validation</strong></p><p>All string fields in the <code>RsyslogRelpConfig</code> API must be validated before use. The current implementation validates the following fields:</p><ul><li><code>target</code>: must be a valid IP address or DNS-1123 subdomain (validated using Kubernetes helpers <code>k8s.io/apimachinery/pkg/util/validation.IsValidIP(...)</code> and <code>k8s.io/apimachinery/pkg/util/validation.IsDNS1123Subdomain(...)</code>).</li><li><code>loggingRules.programNames[]</code>: must contain only printable ASCII characters matching <code>^[!-~]*$</code> and must not contain <code>[</code>, <code>:</code> or <code>/</code>.</li><li><code>tls.permittedPeer[]</code>: must match either the fingerprint format <code>^SHA1:[0-9A-Fa-f]{40}$</code> or be a valid hostname (DNS-1123 subdomain, wildcards allowed).</li><li><code>loggingRules.messageContent.regex</code> and <code>loggingRules.messageContent.exclude</code>: must be valid POSIX Extended Regular Expressions (validated via <code>regexp.CompilePOSIX</code>).</li><li><code>tls.secretReferenceName</code> and <code>auditConfig.configMapReferenceName</code>: must be non-empty strings when the respective feature is enabled.</li></ul><p><strong>String Escaping</strong></p><p>All string fields used in the generated <code>/var/lib/rsyslog-relp-configurator/rsyslog.d/60-audit.conf</code> configuration file must be properly escaped. The current implementation escapes the following fields using <a href="https://pkg.go.dev/strconv#Quote" target="_blank" rel="noreferrer"><code>strconv.Quote</code></a>:</p><ul><li><code>loggingRules.programNames[]</code>: quoted before insertion into <code>$programname == [...]</code> lists (see <code>computeLogFilters()</code> function).</li><li><code>loggingRules.messageContent.regex</code> and <code>loggingRules.messageContent.exclude</code>: quoted before use inside <code>re_match($msg, ...)</code> expressions (see <code>computeLogFilters()</code> function).</li><li><code>tls.permittedPeer[]</code>: each entry quoted before building <code>tls.permittedpeer=[...]</code> (see <code>getRsyslogTLSValues()</code> function).</li></ul><p>Note: Fields like <code>target</code> and <code>port</code> are placed as <code>&quot;{{ .target }}&quot;</code> and <code>&quot;{{ .port }}&quot;</code> by the configuration template. Combined with the validation above, explicit <code>strconv.Quote</code> is not required for these fields.</p><p><strong>Requirements for Future Development</strong></p><p>Any new string fields added to the <code>RsyslogRelpConfig</code> API must follow the same validation and escaping patterns described above. Use Kubernetes and Gardener validation helpers wherever possible.</p><h4 id="tls-secret" tabindex="-1">TLS Secret <a class="header-anchor" href="#tls-secret" aria-label="Permalink to &quot;TLS Secret&quot;">​</a></h4><p>If <code>tls.enabled</code> is true in the <code>RsyslogRelpConfig</code> specification, then the <code>tls.secretReferenceName</code> must point to a <code>Secret</code> resource in the <code>Shoot</code>&#39;s <code>spec.resources</code> array. This must in turn be a reference to a <code>Secret</code> in the user&#39;s project namespace. This <code>Secret</code> contains the necessary TLS certificates.</p><p>The <code>Secret</code> is validated by the shoot validator admission webhook to ensure that:</p><ul><li>It contains exactly three data keys: <code>ca</code> (certificate authority), <code>crt</code> (client certificate), and <code>key</code> (private key)</li><li>It is marked as immutable</li><li>It contains no extra data entries beyond the three required keys</li></ul><h4 id="auditd-configuration" tabindex="-1">Auditd Configuration <a class="header-anchor" href="#auditd-configuration" aria-label="Permalink to &quot;Auditd Configuration&quot;">​</a></h4><p>The <a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/apis/rsyslog/types_auditd.go" target="_blank" rel="noreferrer"><code>Auditd</code></a> API allows users to specify custom audit rules via the <code>auditRules</code> field. The audit rules are provided as a multi-line string containing rules in the format expected by the auditd daemon.</p><p>The <code>Auditd</code> specification is provided in a ConfigMap in the user&#39;s project namespace. The <code>RsyslogRelpConfig</code>&#39;s <code>auditConfig.configMapReferenceName</code> field must point to a ConfigMap resource in the Shoot&#39;s <code>spec.resources</code> array, which in turn references the actual ConfigMap containing the <code>Auditd</code> specification.</p><p>When <code>auditConfig.enabled</code> is set to true and such a reference is present, the following validation takes place:</p><ol><li>The ConfigMap is validated to be immutable and to contain a single <code>data.auditd</code> entry.</li><li>The <code>data.auditd</code> entry is strictly decoded into an <code>Auditd</code> struct.</li><li>The <code>auditRules</code> field is validated to be non-empty. See <a href="https://github.com/gardener/gardener-extension-shoot-rsyslog-relp/blob/main/pkg/apis/rsyslog/validation/auditd.go" target="_blank" rel="noreferrer">pkg/apis/rsyslog/validation/auditd.go</a> for implementation.</li><li>The audit rules string is written as-is to <code>/var/lib/rsyslog-relp-configurator/audit/rules.d/00_shoot_rsyslog_relp.rules</code> on the shoot nodes (base64-encoded during transport).</li><li>No escaping or quoting is applied to the audit rules content, as it is written directly to a file and not interpolated into shell scripts or other configuration formats.</li></ol><p>The audit rules are validated by the auditd system itself when the <code>/var/lib/rsyslog-relp-configurator/configure-rsyslog.sh</code> script calls <code>augenrules --load</code>. If the rules are invalid, the script logs an error and reports the failure via metrics, but the extension does not preemptively validate the audit rule syntax.</p><p>Users are responsible for ensuring their audit rules conform to the <a href="https://man7.org/linux/man-pages/man8/auditctl.8.html" target="_blank" rel="noreferrer">auditd rules syntax</a>.</p><h4 id="rsyslog-configurator-script" tabindex="-1">Rsyslog Configurator Script <a class="header-anchor" href="#rsyslog-configurator-script" aria-label="Permalink to &quot;Rsyslog Configurator Script&quot;">​</a></h4><p>The certificates and configuration files that are installed under <code>/var/lib/rsyslog-relp-configurator/</code> are copied to the <code>/etc</code> directory by the <code>/var/lib/rsyslog-relp-configurator/configure-rsyslog.sh</code> script. Currently this shell script only uses hardcoded constants and does not have any parameters exposed to the outside. However, if future development adds such functionality, any fields that can be configured from the outside must be properly quoted, to avoid shell expansion and code execution.</p>',43)]))}const g=o(r,[["render",a]]);export{p as __pageData,g as default};
