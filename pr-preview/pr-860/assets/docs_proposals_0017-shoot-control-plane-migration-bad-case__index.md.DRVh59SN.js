import{_ as t,c as o,o as s,a2 as n}from"./chunks/framework.QTfwLiL8.js";const u=JSON.parse('{"title":"0017 Shoot Control Plane Migration Bad Case","description":"","frontmatter":{"github_repo":"https://github.com/gardener/enhancements","github_subdir":"geps/0017-shoot-control-plane-migration-bad-case","params":{"github_branch":"main"},"path_base_for_github_subdir":{"from":"content/docs/proposals/0017-shoot-control-plane-migration-bad-case/README.md","to":"README.md"},"title":"0017 Shoot Control Plane Migration Bad Case","prev":false,"next":false},"headers":[],"relativePath":"docs/proposals/0017-shoot-control-plane-migration-bad-case/_index.md","filePath":"docs/proposals/0017-shoot-control-plane-migration-bad-case/_index.md","lastUpdated":null}'),a={name:"docs/proposals/0017-shoot-control-plane-migration-bad-case/_index.md"};function i(r,e,c,d,h,l){return s(),o("div",null,e[0]||(e[0]=[n(`<h1 id="gep-0017-shoot-control-plane-migration-bad-case-scenario" tabindex="-1">GEP-0017: Shoot Control Plane Migration &quot;Bad Case&quot; Scenario <a class="header-anchor" href="#gep-0017-shoot-control-plane-migration-bad-case-scenario" aria-label="Permalink to &quot;GEP-0017: Shoot Control Plane Migration &quot;Bad Case&quot; Scenario&quot;">​</a></h1><p>The <a href="https://github.com/gardener/enhancements/blob/main/geps/0017-shoot-control-plane-migration-bad-case/07-shoot-control-plane-migration.md#migration-workflow" target="_blank" rel="noreferrer">migration flow</a> described as part of <a href="https://github.com/gardener/enhancements/blob/main/geps/0017-shoot-control-plane-migration-bad-case/07-shoot-control-plane-migration.md" target="_blank" rel="noreferrer">GEP-7</a> can only be executed if both the Garden cluster and source seed cluster are healthy, and the <code>gardenlet</code> in the source seed cluster can connect to the Garden cluster. In this case, the <code>gardenlet</code> can directly scale down the shoot&#39;s control plane in the source seed, after checking the <code>spec.seedName</code> field.</p><p>However, there might be situations in which the <code>gardenlet</code> in the source seed cluster can&#39;t connect to the Garden cluster and determine that <code>spec.seedName</code> has changed. Similarly, the connection to the seed <code>kube-apiserver</code> could also be broken. This might be caused by issues with the seed cluster itself. In other situations, the migration flow steps in the source seed might have started but might not be able to finish successfully. In all such cases, it should still be possible to migrate a shoot&#39;s control plane to a different seed, even though executing the migration flow steps in the source seed might not be possible. The potential &quot;split brain&quot; situation caused by having the shoot&#39;s control plane components attempting to reconcile the shoot resources in two different seeds must still be avoided, by ensuring that the shoot&#39;s control plane in the source seed is deactivated before it is activated in the destination seed.</p><p>The mechanisms and adaptations described below have been tested as part of a PoC prior to describing them here.</p><h2 id="owner-election-copying-snapshots" tabindex="-1">Owner Election / Copying Snapshots <a class="header-anchor" href="#owner-election-copying-snapshots" aria-label="Permalink to &quot;Owner Election / Copying Snapshots&quot;">​</a></h2><p>To achieve the goals outlined above, an &quot;owner election&quot; (or rather, &quot;ownership passing&quot;) mechanism is introduced to ensure that the source and destination seeds are able to successfully negotiate a single &quot;owner&quot; during the migration. This mechanism is based on special <em>owner DNS records</em> that uniquely identify the seed that currently hosts the shoot&#39;s control plane (&quot;owns&quot; the shoot).</p><p>For example, for a shoot named <code>i500152-gcp</code> in project <code>dev</code> that uses an internal domain suffix <code>internal.dev.k8s.ondemand.com</code> and is scheduled on a seed with an identity <code>shoot--i500152--gcp2-0841c87f-8db9-4d04-a603-35570da6341f-sap-landscape-dev</code>, the owner DNS record is a TXT record with a domain name <code>owner.i500152-gcp.dev.internal.dev.k8s.ondemand.com</code> and a single value <code>shoot--i500152--gcp2-0841c87f-8db9-4d04-a603-35570da6341f-sap-landscape-dev</code>. The owner DNS record is created and maintained by reconciling an <code>owner</code> DNSRecord resource.</p><p>Unlike other extension resources, the <code>owner</code> DNSRecord resource is not reconciled every time the shoot is reconciled, but only when the resource is created. Therefore, the owner DNS record value (the owner ID) is updated only when the shoot is migrated to a different seed. For more information, see <a href="https://github.com/gardener/gardener/pull/4307" target="_blank" rel="noreferrer">Add handling of owner DNSRecord resources</a>.</p><p>The owner DNS record domain name and owner ID are passed to components that need to perform ownership checks, such as the <code>backup-restore</code> container of the <code>etcd-main</code> StatefulSet, and all extension controllers. These components then check regularly whether the actual owner ID (the value of the record) matches the passed ID. If they don&#39;t, the ownership check is considered failed, which causes the special behavior described below.</p><blockquote><p><strong>Note:</strong> A previous revision of this document proposed using &quot;sync objects&quot; written to and read from the backup container of the source seed as JSON files by the <code>etcd-backup-restore</code> processes in both seeds. With the introduction of owner DNS records such sync objects are no longer needed.</p></blockquote><p>For the destination seed to actually become the owner, it needs to acquire the shoot&#39;s etcd data by copying the final full snapshot (and potentially also older snapshots) from the backup container of the source seed.</p><p>The mechanism to copy the snapshots and pass the ownership from the source to the destination seed consists of the following steps:</p><ol><li><p>The reconciliation flow (&quot;restore&quot; phase) is triggered in the destination seed without first executing the migration flow in the source seed (or perhaps it was executed, but it failed, and its state is currently unknown).</p></li><li><p>The <code>owner</code> DNSRecord resource is created in the destination seed. As a result, the actual owner DNS record is updated with the destination seed ID. From this point, ownership checks by the <code>etcd-backup-restore</code> process and <a href="/docs/proposals/0017-shoot-control-plane-migration-bad-case/readme/#extension-controller-watchdogs">extension controller watchdogs</a> in the source seed will fail, which will cause the special behavior described below.</p></li><li><p>An additional &quot;source&quot; backup entry referencing the source seed backup bucket is deployed to the Garden cluster and the destination seed and reconciled by the backup entry controller. As a result, a secret with the appropriate credentials for accessing the source seed backup container named <code>source-etcd-backup</code> is created in the destination seed. The normal backup entry (referencing the destination seed backup container) is also deployed and reconciled, as usual, resulting in the usual <code>etcd-backup</code> secret being created.</p></li><li><p>A special &quot;copy&quot; version of the <code>etcd-main</code> Etcd resource is deployed to the destination seed. In its <code>backup</code> section, this resource contains a <code>sourceStore</code> in addition to the usual <code>store</code>, which contains the parameters needed to use the source seed backup container, such as its name and the secret created in the previous step.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  backup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    ...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">408740b8-6491-415e-98e6-76e92e5956ac</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      secretRef</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">etcd-backup</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      ...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    sourceStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">d1435fea-cd5e-4d5b-a198-81f4025454ff</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      secretRef</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">source-etcd-backup</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      ...</span></span></code></pre></div></li><li><p>The <code>etcd-druid</code> in the destination seed reconciles the above resource by deploying a <code>etcd-copy</code> Job that contains a single <code>backup-restore</code> container. It executes the newly introduced <code>copy</code> command of <code>etcd-backup-restore</code> that copies the snapshots from the source to the destination backup container.</p></li><li><p>Before starting the copy itself, the <code>etcd-backup-restore</code> process in the destination seed checks if a final full snapshot (a full snapshot marked as <code>final=true</code>) exists in the backup container. If such a snapshot is not found, it waits for it to appear in order to proceed. This waiting is up to a certain timeout that should be sufficient for a full snapshot to be taken; after this timeout has elapsed, it proceeds anyway, and the reconciliation flow continues from step 9. As described in <a href="/docs/proposals/0017-shoot-control-plane-migration-bad-case/readme/#handling-inability-to-access-the-backup-container">Handling Inability to Access the Backup Container</a> below, this is safe to do.</p></li><li><p>The <code>etcd-backup-restore</code> process in the source seed detects that the owner ID in the owner DNS record is different from the expected owner ID (because it was updated in step 2) and switches to a special &quot;final snapshot&quot; mode. In this mode the regular snapshotter is stopped, the readiness probe of the main <code>etcd</code> container starts returning 503, and one final full snapshot is taken. This snapshot is marked as <code>final=true</code> in order to ensure that it&#39;s only taken once, and in order to enable the <code>etcd-backup-restore</code> process in the destination seed to find it (see step 6).</p><blockquote><p><strong>Note:</strong> While testing our PoC, we noticed that simply making the readiness probe of the main <code>etcd</code> container fail doesn&#39;t terminate the existing open connections from <code>kube-apiserver</code> to <code>etcd</code>. For this to happen, either the <code>kube-apiserver</code> or the <code>etcd</code> process has to be restarted at least once. Therefore, when the snapshotter is stopped because an ownership change has been detected, the main <code>etcd</code> process is killed (using <code>SIGTERM</code> to allow graceful termination) to ensure that any open connections from <code>kube-apiserver</code> are terminated. For this to work, the 2 containers must <a href="https://kubernetes.io/docs/tasks/configure-pod-container/share-process-namespace/" target="_blank" rel="noreferrer">share the process namespace</a>.</p></blockquote></li><li><p>Since the <code>kube-apiserver</code> process in the source seed is no longer able to connect to <code>etcd</code>, all shoot control plane controllers (<code>kube-controller-manager</code>, <code>kube-scheduler</code>, <code>machine-controller-manager</code>, etc.) and extension controllers reconciling shoot resources in the source seed that require a connection to the shoot in order to work start failing. All remaining extension controllers are prevented from reconciling shoot resources via the <a href="/docs/proposals/0017-shoot-control-plane-migration-bad-case/readme/#extension-controller-watchdogs">watchdogs</a> mechanism. At this point, the source seed has effectively lost its ownership of the shoot, and it is safe for the destination seed to assume the ownership.</p></li><li><p>After the <code>etcd-backup-restore</code> process in the destination seed detects that a final full snapshot exists, it copies all snapshots (or a subset of all snapshots) from the source to the destination backup container. When this is done, the Job finishes successfully, which signals to the reconciliation flow that the snapshots have been copied.</p><blockquote><p><strong>Note:</strong> To save time, only the final full snapshot taken in step 6, or a subset defined by some criteria, could be copied, instead of all snapshots.</p></blockquote></li><li><p>The special &quot;copy&quot; version of the <code>etcd-main</code> Etcd resource is deleted from the source seed, and as a result the <code>etcd-copy</code> Job is also deleted by <code>etcd-druid</code>.</p></li><li><p>The additional &quot;source&quot; backup entry referencing the source seed backup container is deleted from the Garden cluster and the destination seed. As a result, its corresponding <code>source-etcd-backup</code> secret is also deleted from the destination seed.</p></li><li><p>From this point, the reconciliation flow proceeds as already described in <a href="https://github.com/gardener/enhancements/blob/main/geps/0017-shoot-control-plane-migration-bad-case/07-shoot-control-plane-migration.md" target="_blank" rel="noreferrer">GEP-7</a>. This is safe, since the source seed cluster is no longer able to interfere with the shoot.</p></li></ol><h2 id="handling-inability-to-access-the-backup-container" tabindex="-1">Handling Inability to Access the Backup Container <a class="header-anchor" href="#handling-inability-to-access-the-backup-container" aria-label="Permalink to &quot;Handling Inability to Access the Backup Container&quot;">​</a></h2><p>The mechanism described above assumes that the <code>etcd-backup-restore</code> process in the source seed is able to access its backup container in order to take snapshots. If this is not the case, but an ownership change was detected, the <code>etcd-backup-restore</code> process still sets the readiness probe status of the main <code>etcd</code> container to 503, and kills the main <code>etcd</code> process as described above to ensure that any open connections from <code>kube-apiserver</code> are terminated. This effectively deactivates the source seed control plane to ensure that the ownership of the shoot can be passed to a different seed.</p><p>Because of this, the <code>etcd-backup-restore</code> process in the destination seed responsible for copying the snapshots can avoid waiting forever for a final full snapshot to appear. Instead, after a certain timeout has elapsed, it can proceed with the copying. In this situation, whatever latest snapshot is found in the source backup container will be restored in the destination seed. The shoot is still migrated to a healthy seed at the cost of losing the etcd data that accumulated between the point in time when the connection to the source backup container was lost, and the point in time when the source seed cluster was deactivated.</p><p>When the connection to the backup container is restored in the source seed, a final full snapshot will be eventually taken. Depending on the stage of the restoration flow in the destination seed, this snapshot may be copied to the destination seed and restored, or it may simply be ignored since the snapshots have already been copied.</p><h2 id="handling-inability-to-resolve-the-owner-dns-record" tabindex="-1">Handling Inability to Resolve the Owner DNS Record <a class="header-anchor" href="#handling-inability-to-resolve-the-owner-dns-record" aria-label="Permalink to &quot;Handling Inability to Resolve the Owner DNS Record&quot;">​</a></h2><p>The situation when the owner DNS record cannot be resolved is treated similarly to a failed ownership check: the <code>etcd-backup-restore</code> process sets the readiness probe status of the main <code>etcd</code> container to 503, and kills the main <code>etcd</code> process as described above to ensure that any open connections from <code>kube-apiserver</code> are terminated, effectively deactivating the source seed control plane. The final full snapshot is not taken in this case to ensure that the control plane can be re-activated if needed.</p><p>When the owner DNS record can be resolved again, the following 2 situations are possible:</p><ul><li>If the source seed is still the owner of the shoot, the <code>etcd-backup-restore</code> process will set the readiness probe status of the main <code>etcd</code> container to 200, so <code>kube-apiserver</code> will be able to connect to <code>etcd</code> and the source seed control plane will be activated again.</li><li>If the source seed is no longer the owner of the shoot, the etcd readiness probe will continue to fail, and the source seed control plane will remain inactive. In addition, the final full snapshot will be taken at this time, for the same reason as described in <a href="/docs/proposals/0017-shoot-control-plane-migration-bad-case/readme/#handling-inability-to-access-the-backup-container">Handling Inability to Access the Backup Container</a>.</li></ul><blockquote><p><strong>Note:</strong> We expect that actual DNS outages are extremely unlikely. A more likely reason for an inability to resolve a DNS record could be network issues with the underlying infrastructure. In such cases, the shoot would usually not be usable / reachable anyway, so deactivating its control plane would not cause a worse outage.</p></blockquote><h2 id="migration-flow-adaptations" tabindex="-1">Migration Flow Adaptations <a class="header-anchor" href="#migration-flow-adaptations" aria-label="Permalink to &quot;Migration Flow Adaptations&quot;">​</a></h2><p>Certain changes to the migration flow are needed in order to ensure that it is compatible with the <a href="/docs/proposals/0017-shoot-control-plane-migration-bad-case/readme/#owner-election--copying-snapshots">owner election</a> mechanism described above. Instead of taking a full snapshot of the source seed etcd, the flow deletes the owner DNS record by deleting the <code>owner</code> DNSRecord resource. This causes the ownership check by <code>etcd-backup-restore</code> to fail, and the final full snapshot to be eventually taken, so the migration flow waits for a final full snapshot to appear as the last step before deleting the shoot namespace in the source seed. This ensures that the reconciliation flow described above will find a final full snapshot waiting to be copied at step 6.</p><p>Checking for the final full snapshot is performed by calling the already existing <code>etcd-backup-restore</code> endpoint <code>snapshot/latest</code>. This is possible, since the <code>backup-restore</code> container is always running at this point.</p><p>After the final full snapshot has been taken, the readiness probe of the main <code>etcd</code> container starts failing, which means that if the migration flow is retried due to an error, it must skip the step that waits for <code>etcd-main</code> to become ready. To determine if this is the case, a check whether the final full snapshot has been taken or not is performed by calling the same <code>etcd-backup-restore</code> endpoint, e.g. <code>snapshot/latest</code>. This is possible if the <code>etcd-main</code> Etcd resource exists with non-zero replicas. Otherwise:</p><ul><li>If the resource doesn&#39;t exist, it must have been already deleted, so the final full snapshot n must have been already taken.</li><li>If it exists with zero replicas, the shoot must be hibernated, and the migration flow must have never been executed (since it scales up etcd as one of its first steps), so the final full snapshot must not have been taken yet.</li></ul><h2 id="extension-controller-watchdogs" tabindex="-1">Extension Controller Watchdogs <a class="header-anchor" href="#extension-controller-watchdogs" aria-label="Permalink to &quot;Extension Controller Watchdogs&quot;">​</a></h2><p>Some extension controllers will stop reconciling shoot resources after the connection to the shoot&#39;s <code>kube-apiserver</code> is lost. Others, most notably the infrastructure controller, will not be affected. Even though new shoot reconciliations won&#39;t be performed by the <code>gardenlet</code>, such extension controllers might be stuck in a retry loop triggered by a previous reconciliation, which may cause them to reconcile their resources after the <code>gardenlet</code> has already stopped reconciling the shoot. In addition, a reconciliation started when the seed still owned the shoot might take some time and therefore might still be running after the ownership has changed. To ensure that the source seed is completely deactivated, an additional safety mechanism is needed.</p><p>This mechanism should handle the following interesting cases:</p><ul><li>The <code>gardenlet</code> cannot connect to the Garden <code>kube-apiserver</code>. In this case, it cannot fetch shoots and therefore does not know if control plane migration has been triggered. Even though the <code>gardenlet</code> will not trigger new reconciliations, extension controllers could still attempt to reconcile their resources if they are stuck in a retry loop from a previous reconciliation, and already running reconciliations will not be stopped.</li><li>The <code>gardenlet</code> cannot connect to the seed&#39;s <code>kube-apiserver</code>. In this case, the <code>gardenlet</code> knows if migration has been triggered, but it will not start shoot migration or reconciliation, as it will first check the seed conditions and try to update the <code>Cluster</code> resource, both of which will fail. Extension controllers could still be able to connect to the seed&#39;s <code>kube-apiserver</code> (if they are not running where the <code>gardenlet</code> is running), and similarly to the previous case, they could still attempt to reconcile their resources.</li><li>The seed components (<code>etcd-druid</code>, extension controllers, etc) cannot connect to the seed&#39;s <code>kube-apiserver</code>. In this case, extension controllers would not be able to reconcile their resources, as they cannot fetch them from the seed&#39;s <code>kube-apiserver</code>. When the connection to the <code>kube-apiserver</code> comes back, the controllers might be stuck in a retry loop from a previous reconciliation, or the resources could still be annotated with <code>gardener.cloud/operation=reconcile</code>. This could lead to a race condition depending on who manages to <code>update</code> or <code>get</code> the resources first. If the <code>gardenlet</code> manages to update the resources before they are read by the extension controllers, they would be properly updated with <code>gardener.cloud/operation=migrate</code>. Otherwise, they would be reconciled as usual.</li></ul><blockquote><p><strong>Note:</strong> A previous revision of this document proposed using &quot;cluster leases&quot; as such an additional safety mechanism. With the introduction of owner DNS records, cluster leases are no longer needed.</p></blockquote><p>The safety mechanism is based on <em>extension controller watchdogs</em>. These are simply additional goroutines that are started when a reconciliation is started by an extension controller. These goroutines perform an ownership check on a regular basis using the owner DNS record, similar to the check performed by the <code>etcd-backup-restore</code> process described above. If the check fails, the watchdog cancels the reconciliation context, which immediately aborts the reconciliation.</p><blockquote><p><strong>Note:</strong> The <code>dns-external</code> extension controller is the only extension controller that neither needs the shoot&#39;s <code>kube-apiserver</code>, nor uses the watchdog mechanism described here. Therefore, this controller will continue reconciling <code>DNSEntry</code> resources even after the source seed has lost the ownership of the shoot. With the PoC, we manually delete the <code>DNSOwner</code> resources from the source seed cluster to prevent this from happening. Eventually, the <code>dns-external</code> controller should be adapted to use the owner DNS records to ensure that it disables itself after the seed has lost the ownership of the shoot. Changes in this direction have already been agreed and relevant PRs proposed.</p></blockquote>`,34)]))}const b=t(a,[["render",i]]);export{u as __pageData,b as default};
