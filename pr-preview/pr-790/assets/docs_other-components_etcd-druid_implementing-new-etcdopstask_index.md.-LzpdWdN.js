import{_ as t,c as o,o as r,a2 as a}from"./chunks/framework.Gi3Q71dA.js";const u=JSON.parse('{"title":"Implementing New Etcdopstask","description":"","frontmatter":{"github_repo":"https://github.com/gardener/etcd-druid","github_subdir":"docs/development","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/other-components/etcd-druid/implementing-new-etcdopstask.md","to":"implementing-new-etcdopstask.md"},"persona":"Developers","title":"Implementing New Etcdopstask","prev":false,"next":false},"headers":[],"relativePath":"docs/other-components/etcd-druid/implementing-new-etcdopstask/index.md","filePath":"docs/other-components/etcd-druid/implementing-new-etcdopstask.md","lastUpdated":null}'),n={name:"docs/other-components/etcd-druid/implementing-new-etcdopstask/index.md"};function s(d,e,i,c,l,h){return r(),o("div",null,e[0]||(e[0]=[a('<h1 id="implementing-a-new-etcdopstask" tabindex="-1">Implementing a New EtcdOpsTask <a class="header-anchor" href="#implementing-a-new-etcdopstask" aria-label="Permalink to &quot;Implementing a New EtcdOpsTask&quot;">​</a></h1><p><code>EtcdOpsTask</code> is designed to be extensible, allowing developers to implement new task types as needed. This guide outlines the steps to create and integrate a new <code>EtcdOpsTask</code> type into the Etcd Druid operator.</p><h2 id="controller-architecture-overview" tabindex="-1">Controller Architecture Overview <a class="header-anchor" href="#controller-architecture-overview" aria-label="Permalink to &quot;Controller Architecture Overview&quot;">​</a></h2><ul><li>A single reconciler (as defined in <code>internal/controller/etcdopstask</code>) reconciles all <code>EtcdOpsTask</code> types.</li><li>The reconciler selects the concrete handler based on <code>spec.config</code> (one-of union) via the task handler registry.</li><li>This keeps lifecycle logic unified while allowing task-specific behavior to live in dedicated handlers.</li></ul><h2 id="steps-to-implement-a-new-etcdopstask" tabindex="-1">Steps to Implement a New EtcdOpsTask <a class="header-anchor" href="#steps-to-implement-a-new-etcdopstask" aria-label="Permalink to &quot;Steps to Implement a New EtcdOpsTask&quot;">​</a></h2><ol><li><p><strong>Define the Task Spec and Status</strong>:</p><ul><li>Extend the <code>EtcdOpsTaskConfig</code> under the <code>EtcdOpsTaskSpec</code> in <a href="https://github.com/gardener/etcd-druid/tree/master/api/core/v1alpha1/etcdopstask.go" target="_blank" rel="noreferrer">api/etcdopstask.go</a> to include a new field for your task configuration under the <code>config</code> union type.</li><li>Add the new task’s config type as a dedicated file under <a href="https://github.com/gardener/etcd-druid/tree/master/api/core/v1alpha1" target="_blank" rel="noreferrer"><code>api/core/v1alpha1/</code></a> (similar to <code>ondemandsnapshot.go</code>). This keeps each task’s CRD schema isolated and maintainable.</li><li>Use either <code>kubebuilder markers</code> or <code>CEL</code> expressions to enforce immutability and validation rules on the new config fields as per requirements.</li></ul></li><li><p><strong>Implement the Task Handler</strong>:</p><ul><li>In <code>internal/controller/etcdopstask/handler/</code>, create a new package for your task (e.g., <code>mynewtask</code>).</li><li>Every <code>Etcdopstask</code> handler must implement the <code>Handler</code> interface as defined in <code>internal/controller/etcdopstask/handler/types.go</code>. The interface has three methods which is expected to be implemented: <ul><li><code>Admit(ctx context.Context) Result</code>: Validates preconditions for the task.</li><li><code>Execute(ctx context.Context) Result</code>: Contains the core logic to perform the task.</li><li><code>Cleanup(ctx context.Context) Result</code>: Cleans up any resources created during task execution.</li></ul></li><li>Each of these methods should return a <code>Result</code> struct which has the fields: <ul><li><code>Requeue</code> (bool): Indicates if the task should be requeued.</li><li><code>Error</code> (error): Any error encountered during processing.</li><li><code>Description</code> (string): A human-readable description of the current state or error.</li></ul></li></ul><blockquote><p>NOTE:</p><ol><li>The <code>Admit</code> method is run when the task is created to validate preconditions. If it fails, the task is marked as <code>Rejected</code>. If it succeeds, the task moves to <code>InProgress</code> state and this method is not called again.</li><li>The <code>Execute</code> method is called repeatedly in case of timeouts/transient errors until the task execution is successful or a non-retryable error occurs. Once this is done the task moves to <code>Succeeded</code> or <code>Failed</code> state respectively.</li><li>The <code>Cleanup</code> method is called when the task reaches a terminal state (Succeeded/Failed/Rejected) to clean up any resources created during the task execution. This can be a no-op if there are no resources to clean up. (Refer the <a href="https://github.com/gardener/etcd-druid/tree/master/internal/controller/etcdopstask/handler/ondemandsnapshot/ondemandsnapshot.go" target="_blank" rel="noreferrer">on-demand-snapshot handler implementation</a> for reference).</li><li>Refer the etcdopstask <a href="https://gitthub.com/gardener/etcd-druid/tree/master/api/core/v1alpha1/etcdopstask.go" target="_blank" rel="noreferrer">api</a> for details regarding the etcdopstask state transitions and status fields.</li></ol></blockquote><ul><li>Transient error handling is to be done via setting the <code>requeue</code> field in the return type from the corresponsing methods with appropriate <code>Error</code> and <code>Description</code> field clearly indicating the reason. Common error codes are defined in <a href="https://github.com/gardener/etcd-druid/tree/master/internal/controller/etcdopstask/handler/types.go" target="_blank" rel="noreferrer">types.go</a>. Introduce new error codes if necessary. This will then be reflected in the <code>LastOperation</code> and/or the <code>LastErrors</code> field in the <code>etcdopstask.status</code> field accordingly.</li><li>Once the <code>requeue</code> field is set to false, the task will move to the next phase or terminal state as applicable.</li><li>Add a constructor function in the new handler package that returns an instance of the handler. The constructor should accept necessary dependencies like clients, loggers, etc., similar to existing handlers. This will then be used in the next step to register the handler with the registry. Refer the <a href="https://github.com/gardener/etcd-druid/tree/master/internal/controller/etcdopstask/handler/ondemandsnapshot/ondemandsnapshot.go#L56-L65" target="_blank" rel="noreferrer">on-demand-snapshot handler constructor</a> for reference.</li></ul></li><li><p><strong>Register the Handler</strong>:</p></li></ol><ul><li>The <code>EtcdOpsTask</code> reconciler maintains a task handler registry. Extend <code>DefaultTaskHandlerRegistry()</code> in <code>internal/controller/etcdopstask/reconciler.go</code> to register your new handler, e.g. <code>registry.Register(&quot;MyNewTask&quot;, mynewtask.New)</code>.</li><li>Update <code>getTaskHandler(...)</code> in the same file to select the new handler based on the corresponding <code>spec.config</code> field (one-of union), similar to the <code>OnDemandSnapshot</code> case.</li></ul><ol start="4"><li><strong>Update the Usage Documentation</strong>:</li></ol><ul><li>Add documentation for the new task type in <code>docs/usage/using-etcdopstask.md</code> under the &quot;Supported Task Types&quot; section.</li><li>Include details about the task type, its configuration options, prerequisites, and expected behavior.</li></ul>',9)]))}const m=t(n,[["render",s]]);export{u as __pageData,m as default};
